apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: unbound
  name: unbound-data
  namespace: private-dns
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
  storageClassName: microk8s-hostpath
